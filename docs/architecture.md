# Architecture

_Placeholder._

Albert is a multi-tenant AI SaaS concierge. The hard part is tenant isolation:
Tenant A must never access Tenant B's data.

Planned services (next phase): backend, admin, modelserver, guardrails, plus
postgres, redis, minio, and vault.

Full diagrams and data flow come in a later phase.
