# Flat-key thresholds file (Owner C). Read by:
#   - evals/rag_eval.py
#   - evals/tool_selection_eval.py
#   - .github/workflows/rag-eval.yml
#
# The canonical, nested thresholds file lives at the repo root
# (`/eval_thresholds.yaml`) and is read by Owner D's `evals/common/thresholds.py`
# for the Phase 6 CI gates. T086 will consolidate the two; until then, keep
# values consistent: anything raised in the root file should be matched here.

hit_at_5: 0.6
mrr: 0.5
tool_selection_accuracy: 0.8
