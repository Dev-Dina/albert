"""Platform (tenant_manager) page modules.

Kept out of Streamlit's auto-discovered ``pages/`` directory so the wrong
role's pages are never enumerated in the sidebar; registered explicitly via
``st.navigation`` (see plan Structure Decision).
"""
